[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prompt2model"
version = "0.1.0"
description = "Language-guided vision model factory with built-in refusal gates"
readme = "README.md"
license = { text = "PolyForm-Noncommercial-1.0.0" }
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.12",
  "PyYAML>=6.0",
  "Pillow>=10.4",
  "torch>=2.5",
  "torchvision>=0.20",
  "transformers>=4.50",
  "scikit-learn>=1.6",
  "onnx>=1.17",
  "onnxruntime>=1.24",
  # onnxruntime.quantization (INT8 PTQ in the compression stage) imports
  # ml_dtypes but onnxruntime does not declare it - pin it explicitly.
  "ml_dtypes>=0.4",
  "optuna>=3.6",
  "ultralytics>=8.0",
  "ray[tune]>=2.10",
  "fvcore>=0.1.5",
  "psutil>=6.1",
]

[project.optional-dependencies]
tune = ["ray[tune]>=2.10"]

[project.scripts]
prompt2model = "prompt2model.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

